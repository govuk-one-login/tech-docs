<!-- userinfo error messages -->

| Error           | Description                       | HTTP status code      |
|-----------------|-----------------------------------|-----------------------|
| `invalid_token` | GOV.UK One Login denied your request as you have an invalid or missing bearer access token.<br><br>To proceed, you must use the `Authorization` header to send the token as a [bearer token][external.bearer-token]. | `401` |
